#!monit
set logfile /var/log/monit.log

check process nodejs with pidfile "/var/run/demoapp.pid"
    start program = "/sbin/start demoapp"
    stop program  = "/sbin/stop demoapp"
    if failed port 3000 protocol HTTP
        request /
        with timeout 10 seconds
        then restart